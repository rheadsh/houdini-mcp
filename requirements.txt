# Installed into a versioned .deps directory using Houdini's Python.
# Upper bounds limit API drift while the isolated target prevents modifying
# packages bundled by SideFX.
mcp>=1.0.0,<2
starlette>=0.37.2,<1
uvicorn>=0.30.0,<1
